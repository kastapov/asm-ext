import { ChartOptions } from '../../../../types/chart/ChartOptions';

export const GeneralOptions: Partial<ChartOptions> = {
  chart: {
    zoom: {
      enabled: false
    }
  },
  yaxis: {
    labels: {
      show: false,
    }
  },
}
